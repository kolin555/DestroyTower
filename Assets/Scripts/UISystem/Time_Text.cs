using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class Time_Text : MonoBehaviour
{
  /*  private TextMeshProUGUI time_Text;
    private Color fontcolor;
    
    private void Awake()
    {
        time_Text = this.gameObject.GetComponent<TextMeshProUGUI>();
        fontcolor = time_Text.color;
    }
    // Update is called once per frame
    private void Update()
    {
        
        var countDownTime = LevelContoller.levelInstance.time_Down;
   
        int mins = (int)(countDownTime / 60);
        int seconds = (int)(countDownTime % 60);
        time_Text.text = mins + ":" + seconds;
        if (countDownTime < 10)
        {
            time_Text.color = Color.red;
        }
        else
        {
            time_Text.color = fontcolor;
        }

    }*/
}
